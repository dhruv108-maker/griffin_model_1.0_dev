## 2025-05-20 - Disabled Action Clarity in Upload Workflows
**Learning:** Buttons disabled due to missing prerequisite form inputs (e.g. uploaded files) should explicitly communicate what is missing via `title`, `aria-label`, or helper text so users and screen readers understand why an action cannot be taken.
**Action:** Always provide explicit disabled reasons and screen-reader accessible labels when action buttons depend on required upload inputs.
